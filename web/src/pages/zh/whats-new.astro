---
import Layout from '../../layouts/Layout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import Giscus from '../../components/Giscus.astro';

const lang = 'zh' as const;
const VERSION = '3.6.2';
const baseUrl = `https://github.com/zhitongblog/solomd/releases/download/v${VERSION}`;
---

<Layout
  title="SoloMD v3.6 新功能"
  description="实时编辑模式下的图片 / 表格内联渲染（#44）—— 一直留着的缺口补齐了。同时打包 5 处打磨：AI 按钮跳转、PNG 导出尺寸、可选水印、文件夹记忆、Mac 工具栏溢出。v3.6.1 同日 hotfix 回退两个 v3.6.0 出去后才发现的回归 —— 图片导出和工具栏下拉都恢复正常。"
  lang={lang}
>
  <Header lang={lang} />
  <main>
    <article class="container whats-new">
      <nav class="breadcrumbs">
        <a href="/zh/">首页</a> / <span>新功能</span>
      </nav>

      <p class="version-row">
        <span class="version-tag">v{VERSION}</span>
        <time>2026 年 4 月</time>
      </p>
      <h1>实时编辑下的块级渲染，加 5 处打磨。</h1>
      <p class="lead">
        v3.6 补齐 v2.3 起一直留着的实时编辑缺口（<strong>整行图片
        和 GFM 表格的内联渲染</strong>，issue #44），并打包了 v3.5.5
        之后用户反馈攒下的 <strong>5 处打磨</strong>。v3.6.1 是
        v3.6.0 当天发现的两个回归的 hotfix —— 都已经完整回退，详见下方。
      </p>

      <div class="cta-row">
        <a class="btn btn--primary btn--lg" href={`${baseUrl}/SoloMD_${VERSION}_universal.dmg`}>⬇ 下载 v{VERSION} (Mac)</a>
        <a class="btn btn--secondary btn--lg" href={`${baseUrl}/SoloMD_${VERSION}_x64_en-US.msi`}>下载 (Windows)</a>
        <a class="btn btn--secondary btn--lg" href={`https://github.com/zhitongblog/solomd/releases/tag/v${VERSION}`}>所有平台 →</a>
      </div>

      <section>
        <h2>🔨 #44 —— 实时编辑下的图片 / 表格块</h2>
        <p>
          实时编辑模式下，markdown 源码自带格式实时渲染。但有两类
          块级元素之前没渲染：
        </p>
        <ul>
          <li>整行只是一张图片（<code>![alt](url)</code>，前后没有别的文字）的行，会折叠成一个真的 <code>&lt;img&gt;</code>。图片 src 走预览面板同款的解析管线。</li>
          <li>表头 + 分隔线 + 至少一行内容的 GFM 表格块，会折叠成真正的 <code>&lt;table&gt;</code>，markdown-it 渲染。</li>
        </ul>
        <p>
          光标进入匹配区任意一行 → widget 立刻卸载，源码恢复。不需要
          切换视图模式。
        </p>
      </section>

      <section>
        <h2>✅ 5 处打磨</h2>
        <ol>
          <li><strong>AI 按钮跳错设置位置。</strong>工具栏 AI 没启用时跳转，现在直接落到「集成」分类，不再是默认的「基础」。</li>
          <li><strong>图片导出按内容裁剪。</strong>短笔记导出后高度严格匹配实际渲染区域，不再有底部留白。</li>
          <li><strong>"Created with SoloMD" 水印做成选项。</strong>默认开（适度宣传），<strong>设置 → 导出</strong>可以关。</li>
          <li><strong>文件夹选择器记住上次工作区。</strong>系统对话框现在打开在 SoloMD 已经加载的工作区位置。</li>
          <li><strong>Mac 窄窗口工具栏溢出。</strong>右侧关键按钮（AI / Sponsor / 设置）在任意窗口宽度都能触达。<em>（注意：这条修复在 v3.6.1 被回退了 —— 见下方。）</em></li>
        </ol>
      </section>

      <section>
        <h2>🩹 v3.6.1 hotfix</h2>
        <p>
          有两个回归 v3.6.0 自测时漏掉了。都在 v3.6.1 完整回退，
          同日发出：
        </p>
        <ul>
          <li>
            <strong>图片导出空白。</strong>所有平台都炸了，不只是 Win。
            v3.6 给 html2canvas 显式传了
            <code>windowWidth/Height</code>，跟 article 在
            <code>left: -10000px</code> 的 off-screen 渲染技巧冲突，
            导出回来是空白 PNG。回退到 html2canvas 默认行为；
            "短笔记底部留白"的需求其实早被 watermark 选项覆盖了
            （footer + bottom padding 都跟着开关变）。
          </li>
          <li>
            <strong>工具栏下拉点击穿透到编辑器。</strong>所有下拉
            菜单 —— 新建 / 打开 / 插入 / 复制 / 导出 —— 弹出来但点击
            落到编辑器上。原因是为修 #181（Mac 窄窗口）加在
            <code>.toolbar</code> 上的 <code>overflow-x: auto</code>，
            浏览器把 <code>overflow-y</code> 一并降级成 clip，绝对
            定位的下拉被剪掉。回退；#181 改用
            <code>&lt;Teleport&gt;</code> 的方案排到 v3.7。
          </li>
        </ul>
        <p>
          自测从此变成 tag push 之前的硬性流程 —— v3.6.1 出去前两个
          flow 都在真实浏览器里跑过。
        </p>
      </section>

      <section>
        <h2>🔮 下一步 —— v3.7</h2>
        <p>
          排首位：用 <code>&lt;Teleport&gt;</code> 重做 Mac 窄窗口
          菜单修复；iPad / Mac App Store 重新提交；以及
          <a href="https://github.com/zhitongblog/solomd/discussions">Discussions</a>
          + <a href="https://github.com/zhitongblog/solomd/issues">issues</a>
          里下一周期攒到的内容。
        </p>
      </section>

      <p class="thanks-row">
        感谢 <strong>@beihai23</strong>（v3.5.5 PR #48）和
        <strong>@sparkdonn-netizen</strong>（#43 截图）。早期版本 →
        <a href="/zh/blog/">浏览博客 →</a>
      </p>
    </article>
    <Giscus lang={lang} />
  </main>
  <Footer lang={lang} />
</Layout>

<style>
  .whats-new { max-width: 820px; padding: 40px 20px 80px; }
  .breadcrumbs { font-size: 13px; color: var(--text-muted); margin-bottom: 16px; }
  .breadcrumbs a { color: inherit; }
  .version-row { display: flex; align-items: center; gap: 12px; margin: 0 0 14px; }
  .version-tag {
    display: inline-block;
    font-family: var(--font-mono);
    font-size: 11px;
    font-weight: 700;
    padding: 4px 10px;
    border-radius: 999px;
    background: var(--accent);
    color: #000;
    letter-spacing: 0.04em;
    text-transform: uppercase;
  }
  .whats-new time { font-size: 13px; color: var(--text-muted); }
  .whats-new h1 { font-size: clamp(2.2rem, 5vw, 3.4rem); line-height: 1.1; margin: 0 0 22px; }
  .whats-new h2 { font-size: clamp(1.5rem, 3vw, 1.9rem); margin: 56px 0 16px; }
  .whats-new .lead { font-size: 1.15rem; color: var(--text-muted); line-height: 1.65; margin-bottom: 28px; }
  .whats-new p { line-height: 1.75; margin: 12px 0; }
  .whats-new ul, .whats-new ol { padding-left: 22px; }
  .whats-new li { margin: 6px 0; line-height: 1.7; }
  .whats-new code {
    background: var(--bg-soft);
    padding: 2px 6px;
    border-radius: 4px;
    font-size: 0.92em;
    font-family: var(--font-mono);
    color: var(--accent-deep);
  }
  .whats-new a:not(.btn) { color: var(--accent); }
  .cta-row {
    display: flex;
    flex-wrap: wrap;
    gap: 12px;
    margin: 28px 0 8px;
  }
  .thanks-row {
    margin-top: 64px;
    padding-top: 22px;
    border-top: 1px solid var(--border);
    font-size: 14px;
    color: var(--text-muted);
  }
</style>
